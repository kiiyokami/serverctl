# Fabric server template
# Copy to k8s/helm/values/servers/<your-name>.yaml and fill in name + nodePort.
#
# Public ports 25565–25568 map to NodePorts 30565–30568.
# Pick any unused port from 30565–30568.

name: ""        # required: unique name for this server
nodePort: 30566 # required: pick an unused port from 30565–30568
image: itzg/minecraft-server:java21  # most 1.20+ mods need Java 21

server:
  type: FABRIC
  version: "1.21.1"  # pin to Java 21-compatible MC (LATEST may pull Java 25)
  memory: "8G"
  onlineMode: true
  mods: []

resources:
  requests:
    memory: "8Gi"
    cpu: "1"
  limits:
    memory: "12Gi"
    cpu: "4"

storage: 40Gi

extraEnv:
  JVM_OPTS: "-XX:+UseZGC -XX:+ZGenerational"
